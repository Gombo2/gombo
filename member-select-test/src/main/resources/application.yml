spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/localkick
    driver-class-name: org.mariadb.jdbc.Driver
    username: mozzi1110
    password: tjdmonte080$
  sql:
    init:
      mode: always

mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: org.example.memberservice.domain